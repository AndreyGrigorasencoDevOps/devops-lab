name: CI

on:
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  IMAGE_NAME: task-api
  SMOKE_PORT: 3000
  SMOKE_CONTAINER_PORT: 3000
  SMOKE_HEALTH_PATH: /health

jobs:
  pr-title:
    name: Validate PR title
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: Check semantic PR title
        uses: amannn/action-semantic-pull-request@v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            ci
            chore
            refactor
            test
            build
            perf
            revert
          requireScope: false

  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

  quality:
    name: Quality (Node 24)
    runs-on: ubuntu-latest
    env:
      HAS_SONAR_TOKEN: ${{ secrets.SONAR_TOKEN != '' }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report-node24
          path: coverage/lcov.info

      - name: Sonar scan
        if: ${{ env.HAS_SONAR_TOKEN == 'true' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          args: >
            -Dsonar.projectKey=${{ vars.SONAR_PROJECT }}
            -Dsonar.organization=${{ vars.SONAR_ORG }}
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Security audit (prod deps)
        run: npm audit --omit=dev --audit-level=high

  trivy:
    name: Trivy scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.33.1
        with:
          version: latest
          scan-type: fs
          scan-ref: .
          cache: "false"
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          format: table

      - name: Trivy config scan
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.33.1
        with:
          version: latest
          scan-type: config
          scan-ref: .
          cache: "false"
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          format: table

  smoke:
    name: Docker smoke
    runs-on: ubuntu-latest
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: taskdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d taskdb"
          --health-interval=5s
          --health-timeout=3s
          --health-retries=20
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build container for smoke test
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: local/smoke:node24
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test (/health)
        run: |
          cid=$(docker run -d \
            -p "$SMOKE_PORT:$SMOKE_CONTAINER_PORT" \
            --add-host=host.docker.internal:host-gateway \
            -e NODE_ENV=production \
            -e DB_HOST=host.docker.internal \
            -e DB_PORT=5432 \
            -e DB_USER=postgres \
            -e DB_PASSWORD=postgres \
            -e DB_NAME=taskdb \
            local/smoke:node24)
          trap 'docker rm -f "$cid" 2>/dev/null || true' EXIT
          for i in $(seq 1 30); do
            if curl -fsS --max-time 2 "http://127.0.0.1:$SMOKE_PORT$SMOKE_HEALTH_PATH"; then
              exit 0
            fi
            echo "Attempt $i/30 failed, retrying in 2s..."
            sleep 2
          done
          echo "--- docker ps -a ---"
          docker ps -a
          echo "--- docker logs $cid ---"
          docker logs "$cid"
          exit 1

  ci-summary:
    name: CI summary
    if: always()
    needs: [pr-title, dependency-review, quality, trivy, smoke]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Publish PR CI summary
        run: |
          echo "## CI (PR) Execution Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "| --- | --- |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Event | \`${{ github.event_name }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| PR | \`#${{ github.event.pull_request.number }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Source branch | \`${{ github.head_ref }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| PR title check | \`${{ needs.pr-title.result }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Dependency review | \`${{ needs.dependency-review.result }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Quality (lint/test/sonar/audit) | \`${{ needs.quality.result }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Trivy | \`${{ needs.trivy.result }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Docker smoke | \`${{ needs.smoke.result }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Quality runs on Node 24." >> "$GITHUB_STEP_SUMMARY"
          echo "Docker smoke validates the default \`node:24-alpine\` image." >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Sonar scan runs when \`SONAR_TOKEN\` exists and PR is from this repository." >> "$GITHUB_STEP_SUMMARY"
